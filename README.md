# social-media

A premium Social Media application MVP.

## Architecture & Stack
- **Frontend:** React Native (Expo SDK) + Apollo Client + Pusher WebSocket client
- **Backend:** NestJS + GraphQL (Code-first) + Mongoose (MongoDB) + Redis cache + Pusher/Apinator integration + Cloudinary upload
- **Containers:** MongoDB and Redis managed via Docker Compose
